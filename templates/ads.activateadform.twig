    <section id="portfolio" class="bg-light-gray">
        <div class="container">
            <div class="row">
                <div class="col-lg-12 text-center">
                    <h2 class="section-heading">Aktivera annons</h2>
                    <h3 class="section-subheading text-muted">{{ad.title}}</h3>
                </div>
            </div>
			<div class="container">
				<div class="row">
					<div class="col-xs-12">
						<form action="/ads/activatead/" method="post">
							<div class="form-group">

							<input type="hidden" name="id" value="{{ad.id}}" />

								<label for="date_expire"><h4>Annonsen tas ner</h4></label>
								<p id="date_expire">{{newExpireDate}}</p>			
							</div>
							<div class="form-group">
								<button type="submit" class="btn btn-primary">Aktivera annons</button>
							</div>
							<div class="form-group">
								<a href="/ads/editadform">Redigera annons</a>
						</form>
					</div>
				</div>
			</div>
		</div>
</section>